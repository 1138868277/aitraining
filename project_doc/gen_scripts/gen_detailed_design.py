
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""详细设计文档生成脚本 - 华电新能源测点编码管理平台"""
import sys
sys.path.insert(0, "/Users/liuhaojun/Documents/AI培训/04 脚手架/aitraining/project_doc")
from docx_helper import *

doc = create_document("详细设计文档")

# === 封面 ===
add_cover(doc,
    title="详细设计文档",
    subtitle="华电新能源测点编码管理平台",
    version="V1.1",
    date="2026-06-08"
)

# === 目录 ===
add_toc(doc)

# =========================================
# 1. 概述
# =========================================
add_heading1(doc, "1. 概述")

add_heading2(doc, "1.1 编写目的")
add_para(doc, "本文档旨在指导开发人员进行编码实现，明确各模块的内部结构、核心算法、流程逻辑和数据结构。通过本文档，开发人员可以清晰地了解每个功能模块的实现方式、处理流程、边界条件以及异常处理策略，从而高效、准确地完成编码工作。")
add_para(doc, "本文档是需求规格说明书的细化与补充，将需求层面的功能描述转化为可执行的详细设计说明，涵盖模块内部的类结构、方法定义、算法实现、数据流转以及接口规范等。")

add_heading2(doc, "1.2 适用范围")
add_para(doc, "本文档适用于华电新能源测点编码管理平台的12个功能模块的详细设计，具体包括：")
add_bullet(doc, "编码生成模块：条件驱动编码生成、批量组合生成、自动编码匹配")
add_bullet(doc, "编码校验模块：编码解析、重复稽核、编码修正")
add_bullet(doc, "字典管理模块：字典树展示、快速检索、手动新增")
add_bullet(doc, "字典审批模块：区域提交、集团审批、全租户分发")
add_bullet(doc, "统计分析模块：编码生成统计、全量测点统计、图表可视化")
add_bullet(doc, "时序稽核规则模块：死值/突变/越限/终端规则生成")
add_bullet(doc, "认证与权限模块：JWT登录、路由守卫、多租户鉴权")
add_bullet(doc, "数据源管理模块：配置管理、热切换、锁定保护")
add_para(doc, "本文档面向的开发人员应具备Node.js/TypeScript后端开发能力，熟悉Vue 3前端开发框架，并对PostgreSQL数据库有一定了解。")

add_heading2(doc, "1.3 术语与缩写")
add_table(doc,
    ["术语/缩写", "全称", "说明"],
    [
        ["测点编码", "Measurement Point Code", "新能源设备测点的唯一标识，由31位字母数字组成"],
        ["编码段位", "Code Segment", "31位编码中按业务含义划分的独立片段，共11个段位"],
        ["字典树", "Dictionary Tree", "四级树形结构展示字典项的层级关系，支持缓存加载"],
        ["多租户", "Multi-Tenant", "多个租户共用同一套系统，数据通过PostgreSQL Schema隔离"],
        ["TTL", "Time-To-Live", "缓存存活时间，过期后自动刷新"],
        ["JWT", "JSON Web Token", "用户身份凭证，采用HS256算法签名"],
        ["TSR", "Time Series Rules", "时序稽核规则，包括sz/tb/yx/zd四类"],
        ["Schema", "Database Schema", "PostgreSQL模式，用于实现租户级数据隔离"],
        ["笛卡尔积", "Cartesian Product", "批量编码生成时多个条件可选值的组合算法"],
        ["DAO", "Data Access Object", "数据访问对象，封装数据库查询操作"],
        ["RESTful", "Representational State Transfer", "一种API设计风格，使用HTTP方法操作资源"],
    ],
    col_widths=[2.5, 4, 8.5]
)

add_para(doc, "--- 第一章结束 ---", bold=True, indent=False)

# =========================================
# 2. 编码生成模块详细设计
# =========================================
add_heading1(doc, "2. 编码生成模块详细设计")

add_heading2(doc, "2.1 模块概述")
add_para(doc, "编码生成模块是平台的核心功能模块，提供基于11类字典筛选条件的编码生成引擎。该模块支持单条条件驱动生成、批量组合生成（多选二级类码笛卡尔积 + 扩展码范围组合）以及Excel导入自动编码匹配三种生成模式。同时提供草稿暂存、最近条件复用和快速检索等辅助功能。")
add_para(doc, "模块对应的后端目录为 apps/backend/src/modules/code-generation，前端页面为 /code-generate。该模块涉及的主要数据库表为 cec_new_energy_createcode（编码生成记录表）和 cec_new_energy_recent_condition（最近条件记录表）。")

add_heading2(doc, "2.2 核心流程")
add_para(doc, "编码生成的核心业务流分为以下步骤：")
add_para(doc, "Step 1 - 条件选择：用户进入编码生成页面，逐层选择筛选条件。主要条件包括场站（stationCode）、发电类型（typeCode）、项目线（projectLine）、前缀（prefix）、一级类码（firstClass）、二级类码（secondClass）、三级类码（thirdClass）、数据类码（dataCategory）、数据码（dataCode）等。必选条件为场站、发电类型和二级类码，生成按钮在条件未选全时置灰不可点击。")
add_para(doc, "Step 2 - 条件校验：前端对已选条件的完整性进行校验，确保必选条件均已选择。同时支持条件锁定功能，锁定后切换其他条件时该值保持不变，方便固定某些维度调整其他维度生成。")
add_para(doc, "Step 3 - 编码拼接：后端根据已选条件，按预定义的31位编码段位规则将各段位值拼接为完整编码。如果存在多值条件（如二级类码多选），则自动进行笛卡尔积组合生多条编码。如果设置了扩展码范围（起始值和延伸数量），则生成连续编号范围内的编码组合。")
add_para(doc, "Step 4 - 编码展示：生成的编码在前端以列表形式展示，每条编码显示完整的31位编码字符串及其各段位的实际值。展示区域支持选择操作（全选/单选），选中后可执行入库保存操作。")
add_para(doc, "Step 5 - 快速检索：在生成页面内嵌快速检索面板，支持数据码关键字模糊搜索和类型筛选（F/G/S）。检索结果可点击\"筛选\"按钮自动填充至生成条件，实现快速定位目标字典项。")
add_para(doc, "Step 6 - 草稿保存：生成的编码在入库前可选择暂存至草稿区。草稿数据存储于后端内存，按x-session-id进行会话隔离。草稿区支持查看、单条删除和批量清空操作。")
add_para(doc, "Step 7 - 入库流程：草稿区或预览区的编码可持久化入库保存至cec_new_energy_createcode表。入库操作生成唯一的批次号（batchNo），用于后续的历史记录查询和统计分析。")

add_heading2(doc, "2.3 编码算法")
add_para(doc, "31位编码由11个段位拼接而成，各段位的编码位置、位数和来源说明如下：")
add_table(doc,
    ["序号", "段位名称", "编码位置", "位数", "字段名", "数据来源"],
    [
        ["1", "场站编码", "第1-4位", "4", "stationCode", "station_dict表，根据用户选择的场站确定"],
        ["2", "类型编码", "第5位", "1", "typeCode", "type_dict表，发电类型（F/G/S-原为Y，现统一为S）"],
        ["3", "项目线", "第6-8位", "3", "projectLine", "project_line_dict表，项目期号/并网线路编码"],
        ["4", "前缀", "第9位", "1", "prefix", "prefix_dict表，编码前缀编号"],
        ["5", "一级类码", "第10-11位", "2", "firstClass", "code_dict表的一级分类"],
        ["6", "二级类码", "第12-14位", "3", "secondClass", "second_class_dict表，按类型域分组"],
        ["7", "二级扩展码", "第15-18位", "4", "secondExt", "批量生成时自动递增，单条可指定"],
        ["8", "三级类码", "第19-21位", "3", "thirdClass", "third_class_dict表，三级分类"],
        ["9", "三级扩展码", "第22-25位", "4", "thirdExt", "批量生成时自动递增，单条可指定"],
        ["10", "数据分类码", "第26-27位", "2", "dataCategory", "data_type_dict表，数据分类编码"],
        ["11", "数据码", "第29-31位", "3", "dataCode", "code_dict表的数据码（第28位为保留位）"],
    ],
    col_widths=[1, 3, 2.5, 1.5, 2.5, 5.5]
)
add_para(doc, "编码算法的核心执行步骤如下：")
add_para(doc, "步骤一：从请求参数中提取各段位的选中值，构建段位值映射表（segmentMap）。必选参数校验：场站编码、类型编码、二级类码必须存在有效值。")
add_para(doc, "步骤二：查询对应字典表验证各段位值的合法性。例如，stationCode必须存在于station_dict表中，secondClass必须存在于second_class_dict表中且属于已选类型域。")
add_para(doc, "步骤三：按固定顺序将各段位值拼接为31位编码字符串。拼接顺序为：stationCode + typeCode + projectLine + prefix + firstClass + secondClass + secondExt + thirdClass + thirdExt + dataCategory + \'0\' + dataCode。注意第28位为保留位，固定填充\'0\'。")
add_para(doc, "步骤四：对拼接后的编码进行完整性校验，确认编码长度为31位。如果编码位数不正确，返回错误提示。")
add_para(doc, "步骤五：检查生成的编码是否已在measurement_points表中存在（即是否重复）。如果已存在，返回重复提示并在前端标记。")
add_para(doc, "步骤六：返回生成的编码及各段位详细信息，前端以分块卡片形式展示。")

print("Sections 2.1-2.3 written")

add_heading2(doc, "2.4 批量生成逻辑")
add_para(doc, "批量编码生成支持两种组合方式：多选二级类码笛卡尔积组合和扩展码范围组合，两种方式可同时使用。")
add_para(doc, "笛卡尔积组合逻辑：当用户选择多个二级类码时，系统自动将每个二级类码与已选的其他条件进行组合。例如，用户选择了二级类码[001, 002]，其他条件固定，则生成两条编码（每个二级类码一条）。如果同时选择了多个三级类码或数据类码，则所有可选值之间进行完整的笛卡尔积组合。")
add_para(doc, "扩展码范围组合逻辑：用户可设置二级扩展码和/或三级扩展码的起始值（start）和延伸数量（count）。系统从起始值开始，逐步递增生成连续的扩展码。例如，起始值为0001，延伸数量为5，则生成扩展码0001、0002、0003、0004、0005。")
add_para(doc, "单次上限控制：系统在生成前根据已选条件计算预计生成数量，如果超过100条上限，则在前端提示用户调整条件。生成数量计算公式：secondClassCount * secondExtCount * thirdExtCount。")
add_para(doc, "批量生成的编码按统一的批次号（batchNo）标识，批次号格式为 YYYYMMDDHHmmss + 6位随机数。同一批次的所有编码共享相同的非多选段位值。")

add_heading2(doc, "2.5 自动编码匹配")
add_para(doc, "自动编码匹配功能支持通过Excel导入设备数据，系统自动匹配字典并批量生成编码。完整流程如下：")
add_bullet(doc, "文件上传：用户上传含设备信息的Excel文件（.xlsx/.xls格式），系统使用multer中间件处理文件上传。后端接收文件后，使用xlsx库解析Excel内容，提取Sheet数据为JSON格式。单次处理上限为20000条。")
add_bullet(doc, "列映射：系统自动检测Excel文件的列头信息，展示列映射向导界面。用户需要将Excel列映射到系统字段（如场站名称、设备类型、二级类码等）。前端以表格形式展示映射关系，支持下拉选择映射目标字段。")
add_bullet(doc, "字典匹配：后端将每行数据按映射关系提取字段值，逐字段查询对应的字典表进行匹配。匹配成功的字段自动填充段位值，匹配失败的字段标记为未识别（红色标记），并在结果中提示。")
add_bullet(doc, "固定参数配置：除了Excel列映射的字段外，用户还需配置固定参数（项目期号、前缀号、一级类码等），这些参数对所有生成的编码统一应用。")
add_bullet(doc, "编码生成：字典匹配完成后，系统按编码算法将各段位值拼接为完整的31位编码。生成结果以表格形式展示，包含原始数据、各段位值、完整编码和匹配状态。用户可逐条查看匹配结果。")
add_bullet(doc, "结果导出：生成的编码结果支持导出为Excel文件，包含原始Excel数据列和生成的编码列。")

add_heading2(doc, "2.6 草稿管理")
add_para(doc, "草稿管理用于临时保存已生成但未入库的编码数据，设计要点如下：")
add_bullet(doc, "存储方式：草稿数据存储于后端Node.js进程的内存中，使用Map数据结构管理。键为x-session-id（由前端在请求头中传入），值为草稿编码列表。")
add_bullet(doc, "会话隔离：不同浏览器标签页/窗口通过x-session-id隔离，每个会话拥有独立的草稿空间。x-session-id由前端在应用初始化时随机生成并存储在localStorage中。")
add_bullet(doc, "容量限制：每个会话的草稿上限为5000条。当草稿数量达到上限时，后续的添加操作返回草稿已满的错误提示。")
add_bullet(doc, "生命周期：草稿数据为临时存储，服务重启后将全部丢失。建议用户及时将草稿入库保存。")
add_bullet(doc, "操作支持：草稿区支持查看列表（分页展示）、单条删除、批量清空和全部入库操作。")
add_bullet(doc, "多轮生成：用户可在同一会话中多次生成编码并暂存至草稿区，草稿自动追加至现有草稿列表末尾。支持对草稿中的编码进行筛选和选择性入库。")

add_heading2(doc, "2.7 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["单次生成超限", "已选条件组合预计生成超过100条", "前端拦截，禁止提交生成请求，提示用户缩小条件范围", "单次编码生成不超过100条，请减少所选条件"],
        ["草稿已满", "草稿区已有5000条数据时再次添加", "禁止添加新草稿，提示用户先处理现有草稿", "草稿区已满（上限5000条），请先入库或清理"],
        ["条件未选全", "必选条件（场站/类型/二级类码）任一缺失", "生成按钮置灰不可点击，缺失条件加红色边框提示", "请选择场站、发电类型和二级类码"],
        ["字典加载失败", "字典表数据为空或数据库连接异常", "返回错误信息，前端展示错误状态，生成功能禁用", "字典数据加载失败，请联系管理员"],
        ["扩展码超出范围", "扩展码起始值+延伸数量超过9999", "前端计算预计数量时校验，超限时提示调整", "扩展码范围超出限制（0000-9999）"],
        ["Excel格式错误", "上传的文件不是有效的.xlsx/.xls格式", "multer文件类型校验拦截，返回格式错误提示", "仅支持.xlsx和.xls格式文件"],
        ["自动编码超限", "Excel数据行数超过20000条", "解析前检查行数，超限时拒绝处理", "单次自动编码处理上限为20000条"],
        ["编码重复生成", "生成的编码已在measurement_points表中存在", "每条编码生成后检查重复，重复编码标记红色提示", "编码 XXX 已存在于全量测点数据中"],
        ["会话ID缺失", "请求头未携带x-session-id", "检查x-session-id，缺失时自动生成新的会话ID", "会话信息缺失，已自动创建新会话"],
        ["服务重启导致草稿丢失", "Node.js进程重启", "草稿数据清空，前端检测到会话ID变化后清理本地草稿引用", "草稿数据已清空（服务重启），请重新生成"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Sections 2.4-2.7 written")

# =========================================
# 3. 编码校验模块详细设计
# =========================================
add_heading1(doc, "3. 编码校验模块详细设计")

add_heading2(doc, "3.1 模块概述")
add_para(doc, "编码校验模块涵盖编码解析、重复编码稽核和编码修正三大核心功能。该模块旨在帮助编码专员对31位编码进行反向解析查询字典含义、批量检测编码是否已在全量测点中存在、以及批量执行段位级编码修正操作。")
add_para(doc, "模块对应的后端目录为 apps/backend/src/modules/code-validation，前端页面为 /code-verify。该模块涉及的核心数据库表包括 cec_new_energy_measurement_points（全量测点表）、code_mapping（编码映射表）和 code_correction（编码修正记录表）。")

add_heading2(doc, "3.2 编码解析流程")
add_para(doc, "编码解析功能将31位编码按预定义的11个段位进行分割，各段位分别查询对应的字典表，返回段位含义和识别状态。解析流程如下：")
add_para(doc, "Step 1 - 输入校验：接收前端传入的31位编码字符串，校验长度是否为31位，字符是否为合法的字母数字组合。校验不通过时返回参数错误。")
add_para(doc, "Step 2 - 段位分割：按预定义的段位规则将31位编码切分为11个段位值。分割规则为：stationCode[0:4], typeCode[4:5], projectLine[5:8], prefix[8:9], firstClass[9:11], secondClass[11:14], secondExt[14:18], thirdClass[18:21], thirdExt[21:25], dataCategory[25:27], reserved[27:28], dataCode[28:31]。")
add_para(doc, "Step 3 - 字典查询：对每个段位值，查询对应的字典表获取含义描述。例如，stationCode查询station_dict表获取场站名称，secondClass查询second_class_dict表获取二级类码名称等。如果某个段位值在字典表中不存在对应的记录，则标记为\"未识别\"。")
add_para(doc, "Step 4 - 结果组装：将各段位的解析结果（段位值、段位名称、字典含义、识别状态）组装为统一结构返回。全部段位均成功识别时返回\"全部识别\"状态，存在未识别段位时返回\"部分未识别\"状态并标记未识别的段位。")

add_heading2(doc, "3.3 段位可视化")
add_para(doc, "编码解析结果在前端以分块卡片形式展示，设计要点如下：")
add_bullet(doc, "分块卡片：11个段位以独立的卡片形式排列展示，每个卡片显示段位名称、段位值和字典含义。不同段位使用不同的背景颜色区分（颜色按照段位类型分配：场站=蓝色系、类型=绿色系、类码=橙色系、扩展码=紫色系、数据码=灰色系）。")
add_bullet(doc, "未识别标记：如果某段位解析失败（字典表中无对应记录），该段位卡片背景标红，并在卡片上显示\"未识别\"标签。同时，整体解析结果标签显示\"部分未识别\"（红色标签）。")
add_bullet(doc, "全部识别：当所有11个段位均成功识别时，整体展示\"全部识别\"绿色标签。")
add_bullet(doc, "编码结构图：在解析结果上方展示31位编码的彩色分段结构图，每个段位用不同颜色的色块表示，色块上显示段位名称和值。鼠标悬停色块时显示该段位的详细信息。")

add_heading2(doc, "3.4 批量解析")
add_para(doc, "批量编码解析支持同时解析多条编码，设计要点如下：")
add_bullet(doc, "输入方式：前端提供textarea输入框，用户按行粘贴编码列表（每行一条31位编码）。输入框右下方实时显示已输入编码数量和已输入字符数。")
add_bullet(doc, "上限控制：单次批量解析上限为1000条。如果输入超过1000条，前端自动截取前1000条并提示用户。")
add_bullet(doc, "解析执行：后端逐条调用单条解析逻辑进行解析，采用循环遍历方式。对于每条编码，按3.2节的解析流程执行段位分割和字典查询。")
add_bullet(doc, "结果展示：批量解析结果在展开式表格中展示。表格主行显示序号、编码、识别状态（绿色\"全部识别\"/红色\"部分未识别\"）和展开按钮。展开行显示该编码的11个段位详情（段位名称、值、字典含义）。")
add_bullet(doc, "一键展开/收起：表格顶部提供\"展开全部\"和\"收起全部\"按钮，可批量控制所有行的段位详情展开/收起状态。")
add_bullet(doc, "导出Excel：批量解析结果支持导出为Excel报表。导出的Excel包含以下列：序号、完整编码、识别状态、以及11个段位各自的名称、值和字典含义。导出使用xlsx库生成，通过响应流直接发送给客户端下载。")

add_heading2(doc, "3.5 重复编码稽核")
add_para(doc, "重复编码稽核功能用于检测一批编码是否已在全量测点数据中存在，设计要点如下：")
add_bullet(doc, "输入方式：前端提供textarea输入框，用户按行粘贴编码列表。输入框右下方实时显示输入数量。单次稽核上限为10000条。")
add_bullet(doc, "稽核流程：后端接收编码列表后，执行去重操作（去除列表中的自身重复项），然后批量查询cec_new_energy_measurement_points表中是否存在相同的编码。查询使用IN语句批量执行：SELECT code FROM cec_new_energy_measurement_points WHERE code IN (...)。")
add_bullet(doc, "自身重复检测：输入的编码列表中的自身重复项也被标记为\"重复\"，避免同一批次内重复提交。检测方式为使用Set数据结构对输入列表进行去重。")
add_bullet(doc, "结果标记：每条编码标记\"重复\"或\"不重复\"状态。绿色标签\"不重复\"表示该编码可以安全使用，红色标签\"重复\"表示该编码已存在于数据库或输入列表中存在重复。")
add_bullet(doc, "汇总统计：结果列表顶部显示汇总标签：\"全部不重复，可以使用\"或\"N条重复，请检查\"。")

add_heading2(doc, "3.6 编码修正")
add_para(doc, "编码修正功能支持通过Excel导入批量执行段位级编码修正，完整流程如下：")
add_bullet(doc, "Excel导入：用户拖拽或点击上传.xlsx/.xls格式文件。文件需包含三列：\"测点编码\"（待修正的31位编码）、\"测点描述\"（测点的文字描述）、\"修改内容\"（描述需要修改的内容）。文件格式验证包括列头校验和数据类型校验。")
add_bullet(doc, "修正内容解析：系统逐行解析\"修改内容\"列，提取需要修改的段位和修改后的值。支持的修改格式为：\"段位名称修改为新值\"，多个修改用逗号或中文逗号分隔。例如：\"二级类码修改为003，数据类码修改为11\"。")
add_bullet(doc, "批量修正执行：根据解析结果，对每条编码执行段位替换操作。替换时保留原编码中未修改的段位值，仅替换需要修改的段位。修正后的编码重新拼接为31位完整编码。")
add_bullet(doc, "新旧对比：修正结果以逐段对比方式展示。旧编码的修改段位显示原值（红色删除线），新编码显示修正后的值（绿色加粗）。支持展开查看逐条详细对比。")
add_bullet(doc, "重复检测：修正后的编码自动与cec_new_energy_measurement_points表比对，检测是否存在重复。重复的修正编码在结果中标记为红色。")
add_bullet(doc, "结果导出：修正完成后支持导出Excel报表，包含：原始编码、修改内容、新旧编码对比、重复检测结果等信息。导出使用流式写入方式，防止内存溢出。")

add_heading2(doc, "3.7 修正内容解析")
add_para(doc, "修正内容解析算法负责将自然语言格式的修改描述转换为结构化的段位修改指令。解析算法如下：")
add_para(doc, "Step 1 - 分割修改项：以逗号（英文逗号或中文逗号）为分隔符，将\"修改内容\"字符串分割为多个独立修改项。例如：\"二级类码修改为003，数据类码修改为11\" 分割为 [\"二级类码修改为003\", \"数据类码修改为11\"]。")
add_para(doc, "Step 2 - 提取段位名称：对每个修改项，使用正则表达式匹配\"(.+?)修改为\"模式，提取段位名称。段位名称支持全称（如\"二级类码\"）和别名（如\"二级扩展码\"/\"二级扩展\"）。支持的段位及别名映射关系在修正解析器中预定义。")
add_para(doc, "Step 3 - 提取新值：对每个修改项，使用正则表达式匹配\"修改为(.+)\"模式，提取修改后的新值。新值去除首尾空格后作为段位的新值。")
add_para(doc, "Step 4 - 验证：对提取的段位名称映射到对应的编码位置，验证新值的长度是否符合段位位数要求。例如，二级类码应为3位，数据类码应为2位。不合法时标记该条修正为\"格式错误\"。")
add_para(doc, "Step 5 - 修正执行：根据解析结果，在原编码的对应位置替换为新值，重新拼接为完整编码。")

add_heading2(doc, "3.8 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["批量解析超限", "输入编码超过1000条", "前端自动截取前1000条，提示用户超出部分被忽略", "批量解析上限为1000条，超出部分已忽略"],
        ["重复稽核超限", "输入编码超过10000条", "前端拦截，提示用户分批处理", "单次稽核上限为10000条，请分批处理"],
        ["编码修正超限", "导入编码超过100000条", "解析前检查行数，超限时拒绝处理", "编码修正上限为100000条"],
        ["编码长度不正确", "输入的编码不是31位", "前端实时校验（maxlength=31），后端二次校验", "编码长度必须为31位"],
        ["编码包含非法字符", "编码包含非字母数字字符", "正则校验 /^[A-Za-z0-9]+$/ 不通过时返回错误", "编码只能包含字母和数字"],
        ["段位解析失败", "段位值在字典表中不存在", "标记该段位为\"未识别\"，返回部分识别结果", "第N段\"XXX\"未识别（字典中不存在）"],
        ["修正内容格式错误", "修改内容不符合\"XX修改为XX\"格式", "解析失败时标记该条为\"格式错误\"，跳过修正", "第N行修改内容格式错误，请检查"],
        ["修正值位数不正确", "修改后的值与段位位数不匹配", "校验新值长度，不匹配时标记错误", "段位\"XXX\"的新值长度应为N位"],
        ["第28位被修改", "用户试图修改保留位（第28位）", "修正解析器忽略对保留位的修改指令", "保留位不支持修改"],
        ["修正后编码重复", "修正后的编码在全量测点表中已存在", "修正执行后查询measurement_points表检查重复", "修正后编码XXX已存在，请确认"],
        ["修正分批处理", "修正数量超过50条", "自动按50条一批分批执行，前端实时显示进度", "正在修正第N/M条（进度：XX%）"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Section 3 written")

# =========================================
# 4. 字典管理模块详细设计
# =========================================
add_heading1(doc, "4. 字典管理模块详细设计")

add_heading2(doc, "4.1 模块概述")
add_para(doc, "字典管理模块提供字典数据的分层树形查询、快速检索、字典项概览统计、手动新增字典项及新增记录查询导出等能力。该模块是编码生成和校验的基础数据支撑模块。")
add_para(doc, "模块对应的后端目录为 apps/backend/src/modules/dict，前端页面为 /code-validate。核心数据库表为 cec_new_energy_code_dict（编码字典表），该表采用5级分类体系存储字典数据。")

add_heading2(doc, "4.2 字典树构建")
add_para(doc, "字典树采用四级树形结构展示字典项的层级关系，设计要点如下：")
add_bullet(doc, "四级结构：类型域 → 二级类码 → 数据类码 → 数据码。类型域为根节点（F/G/S），二级类码为第二层，数据类码为第三层，数据码为叶子节点。")
add_bullet(doc, "缓存加载策略：前三级（类型域、二级类码、数据类码）的数据从内存缓存中加载。缓存在应用启动时从cec_new_energy_code_dict表中读取全量数据构建，TTL为5分钟。缓存失效时自动从数据库重新加载。")
add_bullet(doc, "数据码层懒加载：数据码（叶子节点）采用API懒加载方式，仅在用户展开对应的数据类码节点时通过API从数据库查询。查询参数为类型域+二级类码+数据类码，返回该分类下的所有数据码列表。")
add_bullet(doc, "节点展示信息：每个树节点显示编码、名称、子项数量和来源标识。来源标识区分\"系统预置\"和\"手动新增\"（is_manual=1）。")
add_bullet(doc, "树组件使用Element Plus的el-tree组件实现，支持节点展开/收起、选中和搜索过滤。")

add_heading2(doc, "4.3 树形存储")
add_para(doc, "字典数据基于cec_new_energy_code_dict表存储，采用5级分类体系。表结构设计要点：")
add_para(doc, "该表通过type、second_class、data_class、data_code四个字段定义5级分类路径：第1级类型域（type）→ 第2级一级类码（由编码规则隐含）→ 第3级二级类码（second_class）→ 第4级数据类码（data_class）→ 第5级数据码（data_code）。")
add_para(doc, "查询树形数据时，通过GROUP BY和DISTINCT操作聚合各层级的节点数据。例如，查询所有类型域：SELECT DISTINCT type FROM cec_new_energy_code_dict。查询某类型下的二级类码：SELECT DISTINCT second_class, name FROM cec_new_energy_code_dict WHERE type = 'F'。")

add_heading2(doc, "4.4 快速检索")
add_para(doc, "快速检索功能支持通过关键字模糊搜索快速定位字典条目，设计要点如下：")
add_bullet(doc, "搜索范围：关键字在数据码（data_code）和名称（name）字段中进行模糊搜索。使用SQL LIKE语句实现：WHERE data_code LIKE '%keyword%' OR name LIKE '%keyword%'。")
add_bullet(doc, "类型筛选：搜索时可按设备类型域（F/G/S）进行筛选过滤，缩小搜索范围。类型筛选为可选条件，不选择时搜索所有类型。")
add_bullet(doc, "结果展示：搜索结果以列表形式展示，包含类型域、二级类码、数据类码、数据码和名称信息。每行结果右侧提供\"筛选\"按钮，点击后自动将搜索到的字典项条件填充至编码生成页面。")
add_bullet(doc, "锁定功能：搜索结果支持锁定功能，锁定后切换其他搜索条件时结果保持不变。")

add_heading2(doc, "4.5 字典新增")
add_para(doc, "手动新增字典项功能允许编码专员补充缺失的字典数据，设计要点如下：")
add_bullet(doc, "新增方式：支持单条新增和批量新增。单条新增在弹出对话框逐一填写字段信息后提交。批量新增支持以JSON格式或Excel格式批量导入多条字典项。")
add_bullet(doc, "必填字段：新增字典项时必须填写类型域、二级类码、数据类码、数据码和名称。系统自动检测数据唯一性，防止重复添加。")
add_bullet(doc, "手工标记：新增的字典项自动标记is_manual='1'（系统预置的is_manual='0'或NULL），便于后续追溯和统计手工新增的字典数据。")
add_bullet(doc, "审批同步：新增的字典项通过字典审批流程进行审批。审批通过后（approved状态），系统自动遍历所有租户Schema将该字典项写入各租户的cec_new_energy_code_dict表中。")

add_heading2(doc, "4.6 字典项概览")
add_para(doc, "字典项概览以统计卡片形式展示各维度的字典数据量，设计要点如下：")
add_bullet(doc, "统计维度：按风电（F）、光伏（G）、水电（S）三种类型分别统计。每个类型展示5个数据维度的数量：类型域数、二级类码数、三级类码数（按编码规则中的一级类码+二级类码聚合）、数据类码数、数据码数。")
add_bullet(doc, "统计查询：概览数据通过聚合查询计算，SQL示例：SELECT type, COUNT(DISTINCT second_class) as second_count, COUNT(DISTINCT data_class) as data_class_count, COUNT(*) as data_code_count FROM cec_new_energy_code_dict GROUP BY type。")
add_bullet(doc, "展示方式：概览统计以Element Plus的statistic组件或自定义卡片形式展示，每个类型使用不同的颜色标识（F=蓝色、G=绿色、S=橙色）。")

add_heading2(doc, "4.7 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["字典树缓存未加载", "应用刚启动，缓存尚未加载完成", "首次查询时同步加载缓存，加载完成前返回加载中状态", "字典数据加载中，请稍候..."],
        ["缓存TTL过期", "距离上次缓存加载超过5分钟", "下次查询时自动重新加载缓存，旧缓存继续服务直到新缓存就绪", "（无提示，后台静默刷新）"],
        ["缓存加载失败", "数据库连接异常导致缓存加载失败", "返回缓存加载失败错误，使用上次有效缓存（如有）", "字典缓存加载失败，请刷新页面重试"],
        ["懒加载数据码过多", "某数据类码下有大量数据码", "数据码懒加载时限制每页返回数量（默认20条），支持分页加载更多", "（分页加载，无额外提示）"],
        ["手动新增重复", "新增的字典项已存在（type+second_class+data_class+data_code重复）", "唯一性校验检测到重复时拒绝新增，提示用户", "该字典记录已存在，请勿重复添加"],
        ["快速检索无结果", "关键字搜索无匹配结果", "展示空状态组件，引导用户调整搜索关键字或尝试其他类型", "未找到匹配的字典项，请调整搜索条件"],
        ["类型筛选无数据", "选择的类型下没有任何字典数据", "展示空状态提示，引导用户选择其他类型", "该类型下暂无字典数据"],
        ["数据码查询缓存", "数据码懒加载结果缓存", "数据码查询结果在前端缓存（sessionStorage），退出页面时清除", "（无提示）"],
        ["树节点展开缓慢", "数据码量大的节点展开时加载数据", "显示el-tree的loading状态图标，加载完成后自动展开节点", "（loading状态）"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Section 4 written")

# =========================================
# 5. 字典审批模块详细设计
# =========================================
add_heading1(doc, "5. 字典审批模块详细设计")

add_heading2(doc, "5.1 流程设计")
add_para(doc, "字典审批模块提供区域提交字典新增申请和集团审批处理的协同流程，确保字典数据的变更受控和可追溯。完整流程如下：")
add_para(doc, "Step 1 - 区域提交：区域用户在字典管理页面发起新增申请，填写新增的字典项信息（类型域、二级类码、数据类码、数据码、名称）。提交后，数据首先写入当前区域的Schema草稿表（code_draft）中，初始状态为'draft'。同时，一份副本写入admin Schema的审批表（cec_new_energy_code_approval）中。")
add_para(doc, "Step 2 - admin审批：集团管理员登录系统后，在审批管理页面查看待审批列表。列表展示各区域提交的申请，包含提交区域、字典项信息、提交时间和状态。管理员审核每项申请，可选择\"通过\"或\"驳回\"。驳回时需要填写驳回原因。")
add_para(doc, "Step 3 - 通过处理：审批通过后，系统自动执行全租户Schema写入操作。遍历所有租户Schema（admin/yunnan/fujian等），将审批通过的字典项写入各Schema的cec_new_energy_code_dict表。已存在的数据跳过（防重复）。写入完成后更新审批状态为'approved'。")
add_para(doc, "Step 4 - 驳回处理：审批驳回后，更新审批状态为'rejected'，并记录驳回原因。区域侧草稿表同步更新状态为'rejected'。")

add_heading2(doc, "5.2 状态流转")
add_para(doc, "字典审批的状态流转路径如下：")
add_table(doc,
    ["当前状态", "触发操作", "下一状态", "说明"],
    [
        ["draft", "区域用户提交申请", "submitted", "申请已提交，等待admin审批"],
        ["submitted", "admin审批通过", "approved", "审批通过，字典数据已分发到各租户"],
        ["submitted", "admin驳回", "rejected", "审批被驳回，区域用户可查看驳回原因并重新提交"],
        ["approved", "-", "-", "终态，不可再修改"],
        ["rejected", "-", "-", "终态，区域用户可以重新提交新的申请"],
    ],
    col_widths=[2.5, 3, 2.5, 7]
)
add_para(doc, "状态同步机制：区域Schema的code_draft表与admin Schema的cec_new_energy_code_approval表之间通过申请ID关联。状态变更时两表同步更新：提交时同时写入两个Schema；审批时更新admin Schema的审批表，并通过申请ID同步更新对应区域Schema的草稿表状态。")

add_heading2(doc, "5.3 全租户分发")
add_para(doc, "审批通过后的全租户分发机制设计要点如下：")
add_bullet(doc, "遍历租户：从租户配置列表中获取所有区域租户的Schema名称（yunnan, fujian等），加上admin Schema（public），组成完整的写入目标列表。")
add_bullet(doc, "写入策略：对每个目标Schema，执行INSERT语句写入cec_new_energy_code_dict表。写入时检测唯一性约束（type+second_class+data_class+data_code组合唯一），如果记录已存在则跳过。")
add_bullet(doc, "事务管理：整个分发过程在单个数据库事务中执行。如果任意租户的写入失败，整个事务回滚，审批状态回退到submitted，确保数据一致性。")
add_bullet(doc, "分发记录：分发完成后记录分发日志，包含审批申请ID、分发的租户列表、成功数和跳过数。")

add_heading2(doc, "5.4 重复检测")
add_para(doc, "提交时的重复检测策略：同一用户对同一数据码不能重复提交。检测条件为：提交用户（creator）+ 数据码（data_code）在审批表中的组合唯一。如果检测到已存在处于draft或submitted状态的相同记录，则拒绝提交并提示用户。对于已驳回（rejected）的记录，允许用户重新提交。")

add_heading2(doc, "5.5 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["重复提交", "同一用户提交相同数据码的申请", "查询审批表检测重复，拒绝提交", "您已提交过该数据码的申请，请勿重复提交"],
        ["全租户写入失败", "分发时某个租户的数据库写入异常", "事务回滚，审批状态回退到submitted", "审批通过但数据分发失败，请重试或联系管理员"],
        ["审批表数据倾斜", "待审批数据量过大", "分页查询，每页默认20条，支持按区域和状态筛选", "（分页加载）"],
        ["驳回原因过长", "管理员书写的驳回原因超过字段长度限制", "后端校验长度（上限500字），超长时截断或拒绝", "驳回原因不超过500字"],
        ["区域Schema不存在", "租户配置中某区域Schema尚未创建", "分发时检测Schema是否存在，不存在的Schema自动跳过", "区域XXX的Schema不存在，已自动跳过"],
        ["并发审批", "多个管理员同时审批同一申请", "使用数据库行级锁或乐观锁机制防止并发冲突", "该申请正在被其他管理员处理，请刷新后重试"],
        ["已审批通过的申请再次操作", "管理员对已approved的申请点击审批按钮", "前端按钮置灰禁用，后端校验状态后拒绝处理", "该申请已完成审批，不可重复操作"],
        ["草稿与审批状态不一致", "网络异常导致状态同步失败", "定期同步任务检测不一致状态并修复", "（后台自动修复，无需用户操作）"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Section 5 written")

# =========================================
# 6. 统计分析模块详细设计
# =========================================
add_heading1(doc, "6. 统计分析模块详细设计")

add_heading2(doc, "6.1 模块概述")
add_para(doc, "统计分析模块提供编码生成和全量测点的多维度统计分析能力，通过ECharts图表可视化展示。模块分为编码生成统计和全量测点统计两个子功能。")
add_para(doc, "模块对应的后端目录为 apps/backend/src/modules/statistics，前端页面为 /statistics。")

add_heading2(doc, "6.2 编码生成统计")
add_para(doc, "编码生成统计基于cec_new_energy_createcode表中的数据，支持多维度聚合统计：")
add_bullet(doc, "概览统计：展示4个指标卡片——累计生成总数（COUNT(*)）、今日生成数（WHERE create_tm >= TODAY）、本周生成数（WHERE create_tm >= WEEK_START）、本月生成数（WHERE create_tm >= MONTH_START）。各指标使用标准化SQL查询，TODAY/WEEK_START/MONTH_START由后端根据当前时间计算。")
add_bullet(doc, "类型分布：按type_code字段分组统计（F/G/S/不区分），以统计卡片展示各类型的编码数量。查询SQL：SELECT type_code, COUNT(*) as cnt FROM cec_new_energy_createcode GROUP BY type_code。")
add_bullet(doc, "二级类码分布：按second_class_code字段分组统计。前端提供设备类型筛选器（F/G/S），选择后筛选对应类型的数据进行聚合。查询SQL：SELECT second_class_code, COUNT(*) as cnt FROM cec_new_energy_createcode WHERE type_code = $1 GROUP BY second_class_code ORDER BY cnt DESC。")
add_bullet(doc, "场站分布：按station_code字段分组统计编码生成量。同时查询station_dict表获取场站名称进行展示。查询SQL：SELECT c.station_code, s.name as station_name, COUNT(*) as cnt FROM cec_new_energy_createcode c LEFT JOIN cec_new_energy_station_dict s ON c.station_code = s.code GROUP BY c.station_code, s.name。")
add_bullet(doc, "分组详情列表：按生成批次/分组聚合展示，展开行可查看单条编码详情。支持复选框多选、导出选中和批量删除操作。")

add_heading2(doc, "6.3 全量测点统计")
add_para(doc, "全量测点统计支持导入存量测点Excel文件，自动解析并进行分析：")
add_bullet(doc, "大文件导入：支持超大Excel文件上传（.xlsx/.xls，最大200MB）。后端使用xlsx库的流式读取模式，逐行解析文件内容。自动检测包含测点编码的列（通过列头关键字匹配），提取31位编码。")
add_bullet(doc, "批量插入策略：每次读取500条数据后，执行批量INSERT操作写入cec_new_energy_measurement_points表。使用pg驱动的多值INSERT语法：INSERT INTO ... VALUES (...), (...), (...) ON CONFLICT DO NOTHING。")
add_bullet(doc, "编码段位解析：导入时将31位编码按段位规则拆分，存储至measurement_points表的独立字段中（station_code, type_code, project_line, prefix, first_class, second_class, second_ext, third_class, third_ext, data_category, data_code），避免重复计算。")
add_bullet(doc, "进度展示：前端通过轮询导入接口获取实时进度信息，使用el-progress组件展示百分比进度条。进度信息包括：已处理条数、总条数、预估剩余时间。后端在导入过程中将进度信息写入内存变量供查询。")
add_bullet(doc, "取消支持：导入过程中用户可点击取消按钮。后端设置取消标志（cancel_flag=true），下一批插入前检查标志，若已取消则终止导入并返回当前进度。已插入的数据不做回滚。")
add_bullet(doc, "概览统计：导入完成后，展示总测点数、风电/光伏/其他分类统计卡片和最新导入时间。")

add_heading2(doc, "6.4 编码段位解析")
add_para(doc, "全量测点导入时自动将31位编码按段位拆分存储，各段位与measurement_points表的字段映射如下：")
add_table(doc,
    ["编码位置", "段位名称", "字段名", "位数"],
    [
        ["第1-4位", "场站编码", "station_code", "4"],
        ["第5位", "类型编码", "type_code", "1"],
        ["第6-8位", "项目线", "project_line", "3"],
        ["第9位", "前缀", "prefix", "1"],
        ["第10-11位", "一级类码", "first_class", "2"],
        ["第12-14位", "二级类码", "second_class", "3"],
        ["第15-18位", "二级扩展码", "second_ext", "4"],
        ["第19-21位", "三级类码", "third_class", "3"],
        ["第22-25位", "三级扩展码", "third_ext", "4"],
        ["第26-27位", "数据分类码", "data_category", "2"],
        ["第28位", "保留位", "reserved", "1"],
        ["第29-31位", "数据码", "data_code", "3"],
    ],
    col_widths=[2.5, 3, 3.5, 1.5]
)

add_heading2(doc, "6.5 图表设计")
add_para(doc, "统计分析使用ECharts + vue-echarts进行数据可视化展示，图表设计要点如下：")
add_bullet(doc, "图表类型：主要使用横向柱状图（bar），适用于分类数据的对比展示。柱状图使用ECharts的dataset组件实现数据驱动，支持数据筛选和排序。")
add_bullet(doc, "视觉样式：柱状图使用渐变色填充，通过ECharts的linearGradient实现。从柱底到柱顶颜色渐变（如蓝色渐变 #36A2EB -> #4FC3F7）。柱体右侧添加矩形阴影效果，增强视觉层次感。")
add_bullet(doc, "交互功能：鼠标悬浮在柱体上时显示数值提示框（tooltip），包含分类名称和数值。柱体支持点击交互，点击后可以钻取查看该分类的详细数据。")
add_bullet(doc, "响应式配置：图表宽度自适应父容器大小，在窗口大小变化时自动调用chart.resize()重新渲染。")
add_bullet(doc, '空数据展示：当统计数据为空时，图表区域显示“暂无数据”的占位提示。')

add_heading2(doc, "6.6 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["大文件导入超限", "上传文件超过200MB", "multer文件大小校验拦截", "文件大小超过200MB上限"],
        ["Excel解析失败", "文件格式损坏或非标准Excel", "捕获解析异常，返回错误提示", "Excel文件解析失败，请检查文件格式"],
        ["导入过程中取消", "用户点击取消按钮", "设置取消标志，下一批插入前终止", "导入已取消，已导入N条数据"],
        ["编码段位解析失败", "编码不是31位或包含非法字符", "跳过无效编码记录，记录错误日志", "第N行编码解析失败：编码格式错误"],
        ["批量插入失败", "数据库连接异常导致插入失败", "事务回滚当前批次，记录错误日志", "数据导入异常，请重试"],
        ["统计分析无数据", "cec_new_energy_createcode表为空", "概览卡片显示0，图表区域显示空状态", "暂无编码生成数据"],
        ["统计缓存陈旧", "统计数据缓存未及时刷新", "设置合理的缓存TTL（30秒），关键操作后清除缓存", "（无提示，后台自动刷新）"],
        ["柱状图标签重叠", "分类名称过长导致标签重叠", "设置ECharts的axisLabel.rotate旋转角度或interval间隔", "（自动调整）"],
        ["文件格式不支持", "上传非.xlsx/.xls的文件", "multer文件类型校验拦截", "仅支持.xlsx和.xls格式"],
        ["Excel无有效编码列", "文件中未检测到包含编码的列", "解析列头后检测不通过时提示", "未检测到编码列，请确认文件格式"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Section 6 written")

# =========================================
# 7. 时序稽核规则模块详细设计
# =========================================
add_heading1(doc, "7. 时序稽核规则模块详细设计")

add_heading2(doc, "7.1 模块概述")
add_para(doc, "时序稽核规则（TSR）模块提供从存量设备数据到时序数据质量稽核规则的自动转换能力。运维人员按流水线步骤操作：导入场站数据、导入测点数据、自动生成四类稽核规则、多格式导出。")
add_para(doc, "模块对应的后端目录为 apps/backend/src/modules/tsr，前端页面为 /time-series-rules。该模块使用独立的tsr_*系列表存储数据，与编码业务表解耦。")

add_heading2(doc, "7.2 流程设计")
add_para(doc, "时序稽核规则的生成流程分为四个步骤，每步依赖上一步完成：")
add_bullet(doc, "Step 1 - 导入场站数据：用户上传Excel文件导入场站基础数据（最大200MB）。数据解析后写入tsr_station表。导入进度通过后端内存变量实时跟踪，前端通过轮询获取进度百分比。完成后步骤2解锁。")
add_bullet(doc, "Step 2 - 导入测点数据：用户上传Excel文件导入测点数据（最大200MB）。此步骤必须在场站数据导入完成之后进行（按钮禁用+锁定提示），前置依赖检查不通过时拒绝操作。数据解析后写入tsr_measure_data表。完成后步骤3解锁。")
add_bullet(doc, "Step 3 - 生成规则：系统根据导入的场站和测点数据，自动生成四类稽核规则。此步骤必须在测点数据导入完成之后进行。规则数据写入tsr_standard_list表，四类规则分别写入tsr_import_list_sz、tsr_import_list_tb、tsr_import_list_yx、tsr_import_list_zd表。完成后步骤4解锁。")
add_bullet(doc, "Step 4 - 导出结果：用户选择导出格式导出生成的稽核规则。支持单类型Excel导出、整体ZIP导出和分批拆分导出三种方式。")

add_heading2(doc, "7.3 规则生成算法")
add_para(doc, "系统自动生成四类时序稽核规则，每类规则的算法设计如下：")
add_table(doc,
    ["规则类型", "缩写", "检测目标", "算法说明", "输出字段"],
    [
        ["死值", "sz", "检测滑动窗口内是否保持恒定值", "在测点时序数据上滑动窗口（窗口大小可配置，默认10个采样点），计算窗口内数据的标准差。如果标准差接近0（小于阈值），则判定为死值异常。输出：测点信息、死值起始时间、持续时间", "测点编码、场站编码、死值起始时间、持续时间、窗口大小"],
        ["突变", "tb", "检测数据是否发生异常跳变", "对时序数据计算相邻采样点的差值（一阶差分）。如果差值的绝对值超过预设阈值（基于历史数据统计的3倍标准差），则判定为突变异常。输出：测点信息、突变发生时间、突变幅度", "测点编码、场站编码、突变时间、突变前值、突变后值、突变幅度"],
        ["越限", "yx", "检测数据是否超出合理上下限", "从测点数据中统计正常范围（如P5-P95分位数），设定上下限。检测所有数据点，超出上下限范围的标记为越限异常。上下限阈值根据设备类型和量纲自动计算。输出：测点信息、越限时间、越限值", "测点编码、场站编码、越限时间、越限值、上限值、下限值"],
        ["终端", "zd", "检测数据是否在指定时长内无更新", "对每个测点，计算最后一条数据的时间戳与当前时间的差值。如果差值超过预设阈值（可配置，默认24小时），则判定为终端异常。输出：测点信息、最后更新时间、无更新时长", "测点编码、场站编码、最后数据时间、无更新时长（分钟）"],
    ],
    col_widths=[1.5, 1, 2.5, 5, 5.5]
)

add_heading2(doc, "7.4 导出策略")
add_para(doc, "导出策略支持三种方式：")
add_bullet(doc, "单类型Excel导出：用户选择要导出的规则类型（sz/tb/yx/zd），系统查询对应类型的规则表，将数据写入Excel文件并返回下载。使用xlsx库生成，适用于规则数量较少的情况。")
add_bullet(doc, "整体ZIP导出：全部4种规则类型各生成一个Excel文件，使用archiver库打包为一个ZIP文件下载。ZIP内包含4个文件：sz规则.xlsx、tb规则.xlsx、yx规则.xlsx、zd规则.xlsx。")
add_bullet(doc, "分批拆分导出（Z7逻辑）：当某类型规则数据量过大时，自动按7万条一批拆分为多个子文件，所有子文件打包在ZIP中。拆分规则：第1批1-70000条 → file_1.xlsx，第2批70001-140000条 → file_2.xlsx，以此类推。ZIP内的文件按规则类型分目录存放。")

add_heading2(doc, "7.5 流水线进度跟踪")
add_para(doc, "流水线进度以可视化方式展示，设计要点如下：")
add_bullet(doc, "步骤指示器：使用Element Plus的steps步骤条组件展示4个流程步骤。当前正在执行的步骤为激活状态（蓝色高亮），已完成步骤为已完成状态（绿色对勾），未执行步骤为灰色。")
add_bullet(doc, "实时进度：每个步骤执行过程中，在步骤下方显示实时进度百分比（el-progress组件）。进度信息从后端内存变量中获取。")
add_bullet(doc, "异步轮询：前端使用setInterval定时器（间隔2秒）向后端进度接口发起轮询请求，获取当前步骤的进度百分比。步骤完成后清除定时器。")
add_bullet(doc, "状态同步：后端为每个步骤维护独立的状态变量（status: pending/running/completed/failed）和进度百分比（progress: 0-100）。前端根据状态变量控制步骤条的显示样式。")
add_bullet(doc, "步骤依赖控制：前端通过computed属性（importStationDone/importMeasureDone/generateDone）计算各步骤完成状态，未解锁步骤按钮禁用并显示\"请先完成上一步\"锁定提示。")
add_bullet(doc, "刷新恢复：页面刷新后自动恢复后台正在进行的导入进度和生成进度。checkOngoingImport()函数检测到已完成的导入时，恢复上传百分比为100%，确保刷新后进度不重置为0%。")

add_heading2(doc, "7.6 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["前置步骤未完成", "导入测点数据时场站数据尚未导入", "前端按钮禁用+锁定提示，后端前置依赖检查不通过拒绝操作", "请先完成场站数据导入"],
        ["步骤依赖完整链路", "生成规则时测点数据未导入", "生成按钮禁用+锁定提示", "请先完成测点数据导入"],
        ["文件导入超限", "上传文件超过200MB", "multer文件大小校验拦截", "文件大小超过200MB上限"],
        ["规则生成数据为空", "导入的测点数据无有效记录", "规则生成前检查数据量，为空时提示用户", "测点数据为空，无法生成规则"],
        ["导出数据量过大", "某类型规则超过单Excel上限", "自动采用分批拆分导出策略（Z7逻辑）", "数据量较大，将分批打包导出"],
        ["刷新后进度丢失", "页面刷新导致上传进度重置为0%", "checkOngoingImport恢复已完成步骤进度为100%", "（自动恢复，进度保持100%）"],
        ["流水线中断恢复", "页面刷新或网络中断后恢复", "前端自动查询各步骤状态，恢复进度展示", "（自动恢复进度展示）"],
        ["死值检测窗口不足", "测点采样点数少于窗口大小", "跳过该测点，在结果中标记", "测点XXX采样数不足，已跳过"],
        ["突变阈值计算异常", "所有采样值相同导致标准差为0", "设置默认突变阈值，避免算法报错", "测点XXX阈值计算异常，使用默认阈值"],
        ["ZIP打包内存溢出", "规则数据量极大导致内存不足", "使用流式写入方式，分批写入ZIP", "（流式处理，无提示）"],
        ["规则生成性能问题", "测点数据量大导致规则生成缓慢", "按测点分批处理，每批处理1000个测点", "正在处理第N/M批..."],
        ["数据清空二次确认", "用户点击清空按钮", "弹出二次确认对话框确认后执行", "确定要清空所有数据吗？此操作不可恢复"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Section 7 written")

# =========================================
# 8. 认证与权限模块详细设计
# =========================================
add_heading1(doc, "8. 认证与权限模块详细设计")

add_heading2(doc, "8.1 模块概述")
add_para(doc, "认证与权限模块提供用户登录认证、JWT Token鉴权、多租户数据隔离和用户CRUD管理能力。该模块是所有其他模块的前置依赖，其他模块的API请求均需要先通过JWT认证中间件验证身份。")
add_para(doc, "模块对应的后端目录为 apps/backend/src/modules/auth，后端文件包括 auth.controller.ts、auth.service.ts、auth.domain.ts 以及中间件文件。")

add_heading2(doc, "8.2 登录认证")
add_para(doc, "登录认证采用JWT Token机制实现无状态认证，设计要点如下：")
add_bullet(doc, "登录流程：用户在前端登录页面输入用户名和密码，前端通过POST /api/auth/login接口提交登录请求。后端验证用户名和密码，验证通过后签发JWT Token并返回。")
add_bullet(doc, "密码验证：当前版本密码以明文方式存储在cec_sys_user表中。登录时从数据库中查询用户记录，比对输入密码与存储密码是否一致。比对通过后生成Token。")
add_bullet(doc, "Token签发：使用Node.js内置的crypto模块实现JWT的HS256签名算法。Token生成函数为：base64url(header) + '.' + base64url(payload) + '.' + base64url(signature)。其中signature使用crypto.createHmac('sha256', secret)计算。")
add_bullet(doc, "Token内容：Token的payload中包含以下信息：userId（用户ID）、username（用户名）、displayName（显示名）、tenant（租户编码）、role（角色: admin/user）、iat（签发时间）、exp（过期时间）。")
add_bullet(doc, "有效期：Token有效期为24小时。过期后需重新登录获取新Token。当前版本未实现Token刷新机制。")

add_heading2(doc, "8.3 路由守卫")
add_para(doc, "前端路由守卫使用Vue Router的beforeEach导航守卫实现，设计要点如下：")
add_bullet(doc, "守卫触发：用户在浏览器中访问任意受保护路由时，beforeEach守卫触发。守卫检查localStorage中是否存在JWT Token。")
add_bullet(doc, "未登录处理：如果localStorage中不存在Token，守卫将导航重定向到/login页面，并在URL中携带redirect参数（原始目标路径）。登录成功后，前端读取redirect参数自动跳转回原目标页面。")
add_bullet(doc, "已登录处理：如果localStorage中存在Token，守卫放行导航，允许进入目标页面。同时，Axios请求拦截器在每个API请求的请求头中自动添加Authorization: Bearer <token>。")
add_bullet(doc, "Token过期处理：Axios响应拦截器检测到401状态码时，自动清除localStorage中的Token，并触发跳转到登录页面。")

add_heading2(doc, "8.4 多租户鉴权")
add_para(doc, "多租户鉴权采用Schema级数据隔离 + AsyncLocalStorage上下文传递方案：")
add_bullet(doc, "tenantContextMiddleware：每个HTTP请求进入后端时，中间件从JWT Token的payload中解析tenant字段（租户编码）。将解析后的租户信息存入AsyncLocalStorage的上下文存储中。")
add_bullet(doc, "AsyncLocalStorage传递：使用Node.js内置的async_hooks模块的AsyncLocalStorage类，在请求级隔离存储租户上下文。异步操作（如数据库查询）自动继承父级上下文的租户信息。")
add_bullet(doc, "queryAsTenant函数：数据访问层通过queryAsTenant辅助函数执行SQL查询。该函数自动从AsyncLocalStorage获取当前租户，设置search_path为对应的Schema，然后执行SQL。例如：SET search_path TO yunnan; SELECT * FROM cec_new_energy_code_dict。")
add_bullet(doc, "Schema映射：租户编码到Schema名称的映射关系在config/tenants.ts中配置。admin租户映射到public Schema，yunnan映射到yunnan Schema，fujian映射到fujian Schema。")

add_heading2(doc, "8.5 权限控制")
add_para(doc, "权限控制采用角色分级 + 中间件校验策略：")
add_bullet(doc, "角色定义：系统定义两个角色层级——admin（集团管理员）和user（区域用户）。admin拥有系统管理权限，可以管理用户、审批字典；user只能操作本租户Schema下的数据。")
add_bullet(doc, "requireAdmin中间件：管理类接口（用户管理、字典审批等）注册requireAdmin中间件。中间件从JWT Token中解析role字段，如果不是admin角色则返回403 Forbidden错误。")
add_bullet(doc, "数据范围控制：区域用户的操作范围天然受限于其所属的Schema（租户）。通过queryAsTenant函数自动路由到对应的Schema，不需要额外的SQL层面的权限过滤逻辑。admin用户通过设置search_path为public Schema来操作全局数据。")

add_heading2(doc, "8.6 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["Token过期", "JWT Token超过24小时有效期", "后端返回401状态码，前端自动跳转登录页", "登录已过期，请重新登录"],
        ["Token无效", "Token被篡改或签名验证失败", "JWT验签失败返回401，前端清除无效Token", "身份验证失败，请重新登录"],
        ["用户名不存在", "登录时用户名查询不到记录", "返回统一错误提示（不区分用户不存在还是密码错误）", "用户名或密码错误"],
        ["密码错误", "登录时密码比对不一致", "返回统一错误提示", "用户名或密码错误"],
        ["未授权访问受保护接口", "请求头未携带Token", "auth中间件拦截返回401", "未登录，请先登录"],
        ["非管理员访问管理接口", "user角色用户访问admin接口", "requireAdmin中间件拦截返回403", "权限不足，仅管理员可执行此操作"],
        ["用户重复创建", "创建用户时用户名已存在", "唯一性校验检测到重复时拒绝创建", "用户名已存在，请更换用户名"],
        ["多租户Schema不存在", "租户配置了不存在的Schema", "queryAsTenant检测到Schema不存在时返回500", "租户数据配置异常，请联系管理员"],
        ["AsyncLocalStorage上下文丢失", "异步调用链中断导致上下文丢失", "检测到上下文为空时使用默认Schema（public）", "（使用默认Schema）"],
        ["前端路由守卫与后端鉴权不一致", "localStorage有Token但后端已注销", "API请求返回401，前端清除Token并跳转登录", "登录状态已失效，请重新登录"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Section 8 written")

# =========================================
# 9. 数据源管理模块详细设计
# =========================================
add_heading1(doc, "9. 数据源管理模块详细设计")

add_heading2(doc, "9.1 模块概述")
add_para(doc, "数据源管理模块提供PostgreSQL数据源的可视化配置、连接测试和运行时热切换能力。运维人员可以在系统设置页面查看、配置和切换数据库连接，无需重启服务即可生效。")
add_para(doc, "模块对应的后端目录为 apps/backend/src/modules/datasource，前端页面为 /system-settings。配置持久化于项目根目录下的config/datasource.json文件中。")

add_heading2(doc, "9.2 配置管理")
add_para(doc, "数据源配置管理提供查看、测试、保存和切换功能：")
add_bullet(doc, "查看配置：在系统设置页面查看当前PostgreSQL数据源连接配置，包括主机地址（host）、端口（port）、数据库名（database）、用户名（user）、密码（password）、连接池大小（poolSize）等。配置信息从config/datasource.json文件中读取。")
add_bullet(doc, "测试连接：在配置编辑表单中填写新的数据库连接信息后，点击\"测试连接\"按钮。后端使用新的连接参数创建一个临时连接，执行SELECT 1测试语句验证连接是否可用。测试完成后自动关闭临时连接。测试结果在前端显示成功或失败提示。")
add_bullet(doc, "保存配置：测试连接成功后，用户可保存配置。后端将新的连接参数写入config/datasource.json文件，同时触发热切换机制替换运行时连接池。")

add_heading2(doc, "9.3 热切换机制")
add_para(doc, "热切换机制允许在运行时动态替换PostgreSQL连接池，无需重启服务：")
add_bullet(doc, "切换流程：保存配置时，后端执行以下步骤：1）使用新配置参数创建新的pg Pool实例；2）对新池执行一次SELECT 1验证；3）验证通过后，用新池替换全局的dbPool变量；4）关闭旧的连接池。")
add_bullet(doc, "连接池替换：全局dbPool变量在应用启动时初始化。热切换时，dbPool引用指向新的Pool实例。后续所有查询（包括queryAsTenant函数）自动使用新的连接池，无需修改任何业务代码。")
add_bullet(doc, "旧池关闭：替换为新的连接池后，旧的连接池通过pool.end()方法优雅关闭。end()方法等待所有正在执行的查询完成后关闭连接，确保正在执行的查询不受影响。")
add_bullet(doc, "查询路由：queryAsTenant函数始终使用全局dbPool变量获取数据库连接，不缓存连接池引用。因此热切换后，后续查询自动路由到新的连接池。")

add_heading2(doc, "9.4 锁定保护")
add_para(doc, "配置表单默认处于锁定状态，防止非授权修改：")
add_bullet(doc, "锁定状态：页面首次加载时，配置表单的所有输入框为disabled状态，显示锁定图标。用户无法编辑表单内容。")
add_bullet(doc, "解锁操作：用户点击\"解锁\"按钮，弹出确认对话框。确认后表单变为可编辑状态。解锁操作不涉及后端API，仅在前端控制。")
add_bullet(doc, "自动锁定：保存配置或取消编辑后，表单自动恢复为锁定状态。")

add_heading2(doc, "9.5 边界处理")
add_table(doc,
    ["边界场景", "触发条件", "处理策略", "提示信息"],
    [
        ["连接测试失败", "新配置的数据库连接信息错误", "捕获连接异常，返回失败原因", "连接测试失败：{具体错误信息}"],
        ["热切换失败", "新连接池验证不通过", "保存失败，回滚配置变更，旧池继续服务", "数据源配置保存失败，请检查配置后重试"],
        ["旧池关闭异常", "旧连接池中有长时间运行的查询", "设置关闭超时（30秒），超时后强制关闭", "（无提示，后台静默处理）"],
        ["配置文件写入失败", "文件系统权限不足或磁盘空间不足", "捕获写入异常，返回错误提示配置未保存", "配置文件写入失败，请检查服务器权限"],
        ["配置格式错误", "datasource.json文件格式损坏", "读取时捕获JSON解析异常，使用默认配置", "数据源配置文件格式错误，使用默认配置"],
        ["多次热切换", "短时间内频繁切换数据源", "每5秒最多切换一次，超限时提示", "操作太频繁，请稍后再试"],
        ["表单未解锁直接编辑", "锁定状态下点击输入框", "输入框为disabled状态，无法输入", "（无响应）"],
        ["密码脱敏展示", "查看配置时密码字段", "配置查看接口返回脱敏后的密码（前2位明文+***+后2位明文）", "（脱敏展示）"],
    ],
    col_widths=[2.5, 3.5, 5, 4.5]
)

print("Section 9 written")

# =========================================
# 10. 异常处理设计
# =========================================
add_heading1(doc, "10. 异常处理设计")

add_heading2(doc, "10.1 通用异常处理")
add_para(doc, "系统定义统一的异常处理机制，涵盖各类业务异常和技术异常。所有异常统一通过全局错误处理中间件捕获，返回统一的错误响应格式。")
add_table(doc,
    ["异常类型", "HTTP状态码", "错误码", "提示信息", "触发场景"],
    [
        ["参数缺失", "400", "MISSING_PARAMETER", "请求参数缺失：{参数名}", "必填请求参数未提供"],
        ["参数格式错误", "400", "INVALID_PARAMETER", "参数格式不正确，请检查", "参数值类型或格式不符合要求"],
        ["资源不存在", "404", "RESOURCE_NOT_FOUND", "请求的资源不存在", "查询的数据记录不存在"],
        ["未授权", "401", "UNAUTHORIZED", "未登录或Token已过期", "JWT Token缺失或无效"],
        ["权限不足", "403", "FORBIDDEN", "权限不足，仅管理员可执行此操作", "非admin用户访问管理接口"],
        ["业务规则冲突", "409", "BUSINESS_CONFLICT", "操作与现有数据冲突：{详情}", "如重复提交、重复创建等"],
        ["批量处理超限", "400", "BATCH_LIMIT_EXCEEDED", "单次处理数量超过上限：{上限}条", "批量操作超出预设上限"],
        ["文件格式不支持", "400", "FILE_FORMAT_ERROR", "不支持的文件格式，仅支持.xlsx/.xls", "上传的文件格式不正确"],
        ["文件大小超限", "400", "FILE_SIZE_EXCEEDED", "文件大小超过{上限}", "上传文件超过大小限制"],
        ["系统异常", "500", "SYSTEM_ERROR", "服务器内部错误，请联系管理员", "未捕获的运行时异常或数据库异常"],
        ["数据库连接失败", "500", "DATABASE_ERROR", "数据库连接失败，请检查数据源配置", "数据库连接池获取连接失败"],
        ["缓存加载失败", "500", "CACHE_ERROR", "缓存数据加载失败", "字典树等缓存数据加载异常"],
    ],
    col_widths=[2.5, 2, 2.5, 3.5, 5]
)

add_heading2(doc, "10.2 全局错误处理")
add_para(doc, "全局错误处理中间件是Express中间件链的最后一环，通过4个参数的函数签名（err, req, res, next）识别。处理流程如下：")
add_bullet(doc, "捕获错误：当任何中间件或路由处理函数中抛出错误（throw new Error()或throw new AppError()）时，错误会被传递到全局错误处理中间件。")
add_bullet(doc, "判断错误类型：检查错误是否为AppError实例。AppError是系统定义的基类，继承自Error，包含code（错误码）、message（错误描述）、httpStatus（HTTP状态码）三个核心属性。")
add_bullet(doc, "提取错误信息：如果是AppError实例，从中提取code、message和httpStatus。如果是未知错误类型，返回500内部服务器错误和通用提示。")
add_bullet(doc, "记录日志：记录错误日志，包含请求ID、错误码、错误消息、堆栈信息（仅500错误记录堆栈）。日志级别：400类错误为warn，500类错误为error。")
add_bullet(doc, "返回统一响应：统一返回格式为：{ code: <错误码>, message: \"<错误描述>\", data: null, timestamp: \"<ISO时间>\" }。不向客户端暴露内部错误详情。")

add_heading2(doc, "10.3 错误码分段")
add_para(doc, "错误码按模块分段分配，便于快速定位问题来源：")
add_table(doc,
    ["错误码范围", "模块", "说明"],
    [
        ["00000-00999", "系统通用错误", "通用错误码，如参数校验、权限验证、系统异常等"],
        ["01000-01999", "认证与权限模块（auth）", "登录认证、Token签发验证、用户管理等错误"],
        ["02000-02999", "编码生成模块（code-generation）", "编码生成、条件校验、草稿管理、编码算法等错误"],
        ["03000-03999", "自动编码模块（auto-code）", "Excel导入、列映射、字典匹配、批量生成等错误"],
        ["04000-04999", "编码校验模块（code-validation）", "编码解析、重复稽核、编码修正等错误"],
        ["05000-05999", "字典管理模块（dict）", "字典查询、新增、新增记录管理等错误"],
        ["06000-06999", "字典审批模块（approval）", "提交申请、审批处理、全租户分发等错误"],
        ["07000-07999", "场站管理模块（station）", "场站CRUD、导入导出等错误"],
        ["08000-08999", "统计分析模块（statistics）", "统计查询、大文件导入、图表数据等错误"],
        ["09000-09999", "时序稽核规则模块（tsr）", "数据导入、规则生成、规则导出等错误"],
        ["10000-10999", "数据源管理模块（datasource）", "配置管理、热切换、连接测试等错误"],
    ],
    col_widths=[3, 5, 7]
)

add_heading2(doc, "10.4 前端错误处理")
add_para(doc, "前端全局和局部错误处理策略如下：")
add_bullet(doc, "按钮防重复提交：提交类按钮在点击后立即置为loading状态（el-button的loading属性），请求完成或失败后恢复。防止用户在慢网络下重复点击导致重复提交。")
add_bullet(doc, "请求超时处理：Axios配置请求超时时间。超时后触发error回调，弹出\"请求超时，请重试\"提示。")
add_bullet(doc, "401自动跳转登录：Axios响应拦截器检测到HTTP 401状态码时，自动清除本地Token并跳转到登录页面。")
add_bullet(doc, "错误提示：非401的业务错误（400/404/409/500等）使用Element Plus的ElMessage组件显示错误提示。提示内容为后端返回的message字段。")
add_bullet(doc, "表单校验：前端使用Element Plus的表单校验规则（rules）对用户输入进行即时校验，错误在输入框下方红色提示文字显示。减少无效请求发送。")

print("Section 10 written")

# =========================================
# 11. 接口设计总览
# =========================================
add_heading1(doc, "11. 接口设计总览")

add_heading2(doc, "11.1 接口约定")
add_para(doc, "系统所有API接口遵循以下约定：")
add_bullet(doc, "基础路径：所有接口以 /api 为前缀，例如 /api/auth/login、/api/dict/list。")
add_bullet(doc, "HTTP方法：GET用于查询操作，POST用于创建/提交操作，PUT用于更新操作，DELETE用于删除操作。")
add_bullet(doc, "请求格式：POST/PUT请求使用JSON格式（Content-Type: application/json），文件上传使用multipart/form-data。")
add_bullet(doc, "响应格式：统一响应结构为 { code: number, message: string, data: any, timestamp: string }。code为0表示成功，非0表示错误。")
add_bullet(doc, "认证方式：除登录和健康检查接口外，所有接口需在请求头携带 Authorization: Bearer <token>。")
add_bullet(doc, "分页参数：列表查询接口统一使用 page（页码，从1开始）和 pageSize（每页条数，默认20）参数。响应中的data包含records（数据列表）、total（总条数）、page（当前页码）、pageSize（每页条数）字段。")

add_heading2(doc, "11.2 模块接口汇总")
add_para(doc, "各模块的API接口汇总如下：")

add_heading3(doc, "11.2.1 认证与权限模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/auth/login", "POST", "用户登录认证，返回JWT Token", "公开"],
        ["/api/auth/logout", "POST", "用户退出登录", "登录用户"],
        ["/api/auth/userinfo", "GET", "获取当前用户信息", "登录用户"],
        ["/api/auth/users", "GET", "用户列表查询", "admin"],
        ["/api/auth/users", "POST", "新增用户", "admin"],
        ["/api/auth/users/:id", "PUT", "编辑用户", "admin"],
        ["/api/auth/users/:id", "DELETE", "删除用户", "admin"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.2 编码生成模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/code-generation/generate", "POST", "条件驱动生成单条编码", "登录用户"],
        ["/api/code-generation/batch-generate", "POST", "批量组合生成编码", "登录用户"],
        ["/api/code-generation/draft", "GET", "查询草稿列表", "登录用户"],
        ["/api/code-generation/draft", "POST", "添加编码到草稿", "登录用户"],
        ["/api/code-generation/draft/:sessionId", "DELETE", "清空草稿", "登录用户"],
        ["/api/code-generation/draft/item", "DELETE", "删除单条草稿", "登录用户"],
        ["/api/code-generation/save", "POST", "编码入库保存", "登录用户"],
        ["/api/code-generation/history", "GET", "历史记录分页查询", "登录用户"],
        ["/api/code-generation/history", "DELETE", "批量删除历史记录", "登录用户"],
        ["/api/code-generation/history/export", "GET", "历史记录导出Excel", "登录用户"],
        ["/api/code-generation/recent-conditions", "GET", "最近条件列表查询", "登录用户"],
        ["/api/code-generation/recent-conditions", "POST", "保存最近条件", "登录用户"],
        ["/api/code-generation/quick-search", "GET", "快速检索面板数据查询", "登录用户"],
        ["/api/code-generation/search", "GET", "编码搜索", "登录用户"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.3 自动编码模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/auto-code/upload", "POST", "上传Excel文件并解析", "登录用户"],
        ["/api/auto-code/columns", "GET", "获取可用系统字段列表", "登录用户"],
        ["/api/auto-code/mapping", "POST", "提交列映射配置", "登录用户"],
        ["/api/auto-code/generate", "POST", "执行自动编码生成", "登录用户"],
        ["/api/auto-code/template", "GET", "下载导入模板Excel", "登录用户"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.4 编码校验模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/code-parse/parse", "POST", "单条编码解析", "登录用户"],
        ["/api/code-parse/batch-parse", "POST", "批量编码解析", "登录用户"],
        ["/api/code-parse/export", "POST", "批量解析结果导出Excel", "登录用户"],
        ["/api/code-audit/audit", "POST", "重复编码稽核", "登录用户"],
        ["/api/code-correction/upload", "POST", "上传修正Excel文件", "登录用户"],
        ["/api/code-correction/execute", "POST", "执行批量编码修正", "登录用户"],
        ["/api/code-correction/progress", "GET", "查询修正进度", "登录用户"],
        ["/api/code-correction/export", "POST", "修正结果导出Excel", "登录用户"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.5 字典管理模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/dict/overview", "GET", "字典项概览统计", "登录用户"],
        ["/api/dict/tree", "GET", "字典树数据（前三级）", "登录用户"],
        ["/api/dict/data-codes", "GET", "数据码懒加载查询", "登录用户"],
        ["/api/dict/search", "GET", "快速检索字典项", "登录用户"],
        ["/api/dict/manual-add", "POST", "手动新增字典项", "登录用户"],
        ["/api/dict/manual-records", "GET", "新增记录查询", "登录用户"],
        ["/api/dict/manual-records/export", "GET", "新增记录导出Excel", "登录用户"],
        ["/api/dict/data-codes/list", "GET", "按类型获取数据码列表", "登录用户"],
        ["/api/dict/second-class/list", "GET", "按设备类型获取二级类码列表", "登录用户"],
        ["/api/dict/max-code", "GET", "查询最大编码编号", "登录用户"],
        ["/api/dict/max-data-class", "GET", "查询最大数据类码编号", "登录用户"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.6 字典审批模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/approval/draft", "POST", "区域提交审批申请", "登录用户"],
        ["/api/approval/list", "GET", "审批列表查询", "admin"],
        ["/api/approval/submit-list", "GET", "提交记录查询", "登录用户"],
        ["/api/approval/approve", "POST", "审批通过", "admin"],
        ["/api/approval/reject", "POST", "审批驳回", "admin"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.7 场站管理模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/station/list", "GET", "场站分页查询", "登录用户"],
        ["/api/station", "POST", "新增场站", "登录用户"],
        ["/api/station/:id", "PUT", "编辑场站", "登录用户"],
        ["/api/station/:id", "DELETE", "删除场站", "登录用户"],
        ["/api/station/clear", "POST", "全部清空场站", "登录用户"],
        ["/api/station/import", "POST", "批量导入场站", "登录用户"],
        ["/api/station/export", "GET", "场站导出Excel", "登录用户"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.8 统计分析模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/statistics/overview", "GET", "编码生成概览统计", "登录用户"],
        ["/api/statistics/type-distribution", "GET", "类型分布统计", "登录用户"],
        ["/api/statistics/second-class-distribution", "GET", "二级类码分布统计", "登录用户"],
        ["/api/statistics/station-distribution", "GET", "场站分布统计", "登录用户"],
        ["/api/statistics/trend", "GET", "生成趋势统计", "登录用户"],
        ["/api/statistics/detail-list", "GET", "分组详情列表查询", "登录用户"],
        ["/api/statistics/measurement/import", "POST", "全量测点文件导入", "登录用户"],
        ["/api/statistics/measurement/progress", "GET", "导入进度查询", "登录用户"],
        ["/api/statistics/measurement/cancel", "POST", "取消导入", "登录用户"],
        ["/api/statistics/measurement/overview", "GET", "测点概览统计", "登录用户"],
        ["/api/statistics/measurement/chart", "GET", "测点多维度图表数据", "登录用户"],
        ["/api/statistics/measurement/clear", "POST", "清空测点数据", "登录用户"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.9 时序稽核规则模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/tsr/data-info", "GET", "数据概览统计", "登录用户"],
        ["/api/tsr/import-station", "POST", "导入场站数据", "登录用户"],
        ["/api/tsr/import-measure", "POST", "导入测点数据", "登录用户"],
        ["/api/tsr/generate", "POST", "生成稽核规则", "登录用户"],
        ["/api/tsr/progress", "GET", "查询流水线进度", "登录用户"],
        ["/api/tsr/export/:type", "GET", "单类型规则导出Excel", "登录用户"],
        ["/api/tsr/export-all", "GET", "全部规则导出ZIP", "登录用户"],
        ["/api/tsr/export-split", "GET", "分批拆分导出ZIP", "登录用户"],
        ["/api/tsr/clear", "POST", "清空所有数据", "登录用户"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.10 数据源管理模块接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/datasource/config", "GET", "查看当前数据源配置", "admin"],
        ["/api/datasource/test", "POST", "测试数据库连接", "admin"],
        ["/api/datasource/save", "POST", "保存新数据源配置并热切换", "admin"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

add_heading3(doc, "11.2.11 健康检查与公共接口")
add_table(doc,
    ["接口路径", "方法", "功能说明", "权限"],
    [
        ["/api/health", "GET", "服务健康检查", "公开"],
        ["/api/config/dict", "GET", "获取动态字典配置", "公开"],
    ],
    col_widths=[4, 1.5, 4.5, 2]
)

print("Section 11 written")

# =========================================
# 12. 性能设计
# =========================================
add_heading1(doc, "12. 性能设计")

add_heading2(doc, "12.1 批量限制")
add_para(doc, "系统对各类批量操作的上限进行了统一规划，确保系统资源的合理使用和响应时间的可控。各功能的批量上限如下：")
add_table(doc,
    ["功能", "上限", "说明"],
    [
        ["单次编码生成", "100条", "条件驱动或批量组合生成单次最多生成100条编码，超限时前端提示调整条件"],
        ["自动编码", "20000条", "Excel导入自动编码单次最大处理行数"],
        ["批量解析", "1000条", "批量编码解析单次最大处理条数"],
        ["重复稽核", "10000条", "批量重复编码稽核单次最大处理条数"],
        ["编码修正", "100000条", "批量编码修正单次最大处理条数"],
        ["草稿上限", "5000条", "每个x-session-id对应的草稿存储上限"],
        ["文件上传", "200MB", "Excel导入文件、测点数据文件上传大小上限"],
        ["历史记录导出", "10000行", "历史记录单次Excel导出行数上限"],
        ["分页默认", "20条", "列表分页查询的默认每页条数"],
        ["JSON请求体", "200MB", "后端JSON解析的请求体大小限制"],
        ["常规请求超时", "30秒", "普通API请求的超时时间"],
        ["测点导入超时", "600秒", "全量测点文件导入请求的超时时间"],
    ],
    col_widths=[3, 2, 10]
)

add_heading2(doc, "12.2 缓存策略")
add_para(doc, "系统针对不同数据特性采用差异化的缓存策略：")
add_table(doc,
    ["缓存类型", "存储位置", "数据结构", "TTL", "失效策略", "适用数据"],
    [
        ["字典树缓存", "Node.js进程内存", "Map对象", "5分钟", "TTL到期自动刷新，同时保留旧缓存作为降级服务", "字典树前三级数据（类型域、二级类码、数据类码）"],
        ["草稿缓存", "Node.js进程内存", "Map<sessionId, Draft[]>", "无TTL", "服务重启后丢失，手动清理或会话过期清理", "已生成但未入库的编码草稿"],
        ["缓存中间件", "Node.js进程内存", "Map对象", "30秒", "TTL到期自动失效", "统计分析聚合数据、字典项概览等"],
        ["前端缓存", "sessionStorage", "JSON", "页面会话", "页面关闭或刷新后清除", "数据码懒加载结果、当前会话的草稿引用"],
    ],
    col_widths=[2, 3, 3, 1.5, 4, 4.5]
)

add_para(doc, "缓存使用注意事项：")
add_bullet(doc, "字典树缓存是高频读缓存，几乎每次编码生成和校验操作都需要访问。5分钟的TTL在字典数据很少变更的前提下提供了良好的读性能。")
add_bullet(doc, "草稿缓存存储在进程内存中，服务重启后丢失。建议开发人员考虑将草稿数据增加定期持久化机制（如每10分钟备份到数据库），降低数据丢失风险。")
add_bullet(doc, "缓存中间件用于减轻统计查询对数据库的压力。统计数据的实时性要求不高，30秒的TTL可以接受。")
add_bullet(doc, "所有缓存使用Map数据结构实现，查询时间复杂度为O(1)。无需引入Redis等外部缓存组件，降低系统复杂度。")

add_heading2(doc, "12.3 数据库优化")
add_para(doc, "数据库层面的性能优化策略如下：")
add_heading3(doc, "12.3.1 索引策略")
add_table(doc,
    ["表名", "索引字段", "索引类型", "说明"],
    [
        ["cec_new_energy_createcode", "batch_no", "BTREE", "批次号索引，用于分组查询和统计分析"],
        ["cec_new_energy_createcode", "type_code", "BTREE", "类型编码索引，用于类型分布统计"],
        ["cec_new_energy_createcode", "station_code", "BTREE", "场站编码索引，用于场站分布统计"],
        ["cec_new_energy_createcode", "create_tm", "BTREE", "创建时间索引，用于时间趋势统计和概览查询"],
        ["cec_new_energy_measurement_points", "code", "UNIQUE BTREE", "编码唯一索引，用于重复稽核快速查询"],
        ["cec_new_energy_measurement_points", "second_class", "BTREE", "二级类码索引，用于图表分析"],
        ["cec_new_energy_code_dict", "type, second_class, data_class, data_code", "UNIQUE BTREE", "联合唯一索引，防止字典数据重复"],
        ["cec_sys_user", "username", "UNIQUE BTREE", "用户名唯一索引，用于登录查询"],
        ["cec_new_energy_code_approval", "status", "BTREE", "审批状态索引，用于待审批列表查询"],
        ["cec_new_energy_code_approval", "creator, data_code", "UNIQUE BTREE", "防止同一用户提交重复数据码"],
    ],
    col_widths=[4, 5, 1.5, 4.5]
)

add_heading3(doc, "12.3.2 批量插入策略")
add_para(doc, "大批量数据插入时，采用以下优化策略：")
add_bullet(doc, "批量VALUES语法：使用INSERT INTO table (col1, col2) VALUES ($1, $2), ($3, $4), ($5, $6) ... 的多值语法，每批500条数据。相比逐条INSERT，可减少网络往返次数和事务开销。")
add_bullet(doc, "ON CONFLICT处理：对于可能存在重复的数据（如全量测点导入），使用ON CONFLICT DO NOTHING或ON CONFLICT (code) DO UPDATE SET ... 进行幂等处理。")
add_bullet(doc, "分批事务：每500条数据作为一个独立事务批次。如果某批次插入失败，仅回滚该批次，不影响已成功插入的数据。同时记录失败批次的日志信息，方便排查。")

add_heading3(doc, "12.3.3 分页查询优化")
add_para(doc, "列表分页查询采用LIMIT + OFFSET方式实现，对于大数据量场景的优化建议：")
add_bullet(doc, "所有列表查询接口统一使用 page 和 pageSize 参数，默认每页20条。")
add_bullet(doc, "历史记录、审批列表等大数据量场景的查询配合索引使用，避免全表扫描。")
add_bullet(doc, "对于需要统计总数的查询（如历史记录分页），使用 COUNT(*) 配合条件索引。")
add_bullet(doc, "对于超大数据量场景（如编码修正100000条），前端建议增加筛选条件缩小查询范围，避免单次加载过多数据。")

# =========================================
# 文档结束
# =========================================
add_para(doc, "--- 文档结束 ---", bold=True, indent=False)

# === 保存文档 ===
save_doc(doc, "详细设计文档.docx")
print("\n文档生成完毕！")
